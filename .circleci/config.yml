version: 2
jobs:
  build:
    docker:
    - image: circleci/node:10.11
    working_directory: ~/singnet/platform-contracts
    steps:
    - checkout
    - run:
        name: Install
        command: |
          npm install
    - run:
        name: Test
        command: |
          npm run test
